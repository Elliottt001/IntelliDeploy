1. 用户原始需求
   raw_query
2. 用户需求结构化结果
   target_output_type
   target_app_type
   expected_features
   preferred_language
   preferred_framework
   constraints
3. Top 1 候选 GitHub 仓库
   rank
   retrieval_score
   repo_url
   default_branch
   description
   topics
   stars
   is_archived
   last_commit_at
4. 每个仓库的文件树
   file_tree
5. 每个仓库的关键文件内容
   README
   依赖文件：package.json / requirements.txt / pyproject.toml / pom.xml / go.mod
   lock 文件：package-lock.json / pnpm-lock.yaml / yarn.lock / poetry.lock / uv.lock
   构建文件：Dockerfile / docker-compose.yml / Makefile
   入口文件：main.py / app.py / server.js / index.js / src/main.ts
   配置文件：vite.config.ts / next.config.js / Procfile
