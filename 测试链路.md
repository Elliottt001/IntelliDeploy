# IntelliDeploy MVP 测试链路

更新时间：2026-05-13

适用范围：
- `C:\Users\ROG\Desktop\Deploy\Deploy11\IntelliDeploy\backend`
- 当前 MVP 目标：后端可启动、鉴权可用、Generation 接口可用、WebSocket 会话流可用

说明：
- 这份文档按 Windows PowerShell 编写。
- 当前后端默认强制使用 PostgreSQL，不直接使用 `backend\intellideploy.db`。
- 这份测试链路优先覆盖你们当前负责的多智能体 + WebSocket + 状态机联调部分。

---

## 1. 测试目标

本轮测试要确认以下内容：

1. 后端服务能正常启动。
2. 用户注册、登录、JWT 鉴权可用。
3. `/api/generation/*` 接口能正常响应，并返回符合当前约定的错误结构和 camelCase 字段。
4. `/ws/chat/{sessionId}` 能建立连接，并返回 `phase_update`、`message_append`、`task_done/task_error` 这类会话事件。
5. `/ws/deployments/{deploymentId}` 能建立连接，并返回部署状态事件。
6. `appCard` 和错误码对齐逻辑至少达到 MVP 可验证状态。

---

## 2. 环境准备

### 2.1 准备 `.env`

在路径 `C:\Users\ROG\Desktop\Deploy\Deploy11\IntelliDeploy\backend\.env` 中写入：

```env
DATABASE_URL=postgresql+psycopg://postgres:123456@127.0.0.1:5432/intellideploy
SECRET_KEY=test-secret-key
REDIS_ENABLED=false
FALLBACK_SERVICE_URL=http://127.0.0.1:8001
```

预期结果：
- 后端读取 `.env` 成功。
- 不再因为默认数据库配置而启动失败。

### 2.2 启动 PostgreSQL

如果本机还没有 PostgreSQL，可以先用 Docker 启一个测试库：

```powershell
docker run --name intellideploy-pg `
  -e POSTGRES_PASSWORD=123456 `
  -e POSTGRES_DB=intellideploy `
  -p 5432:5432 `
  -d postgres:16
```

预期结果：
- 容器启动成功。
- 本机 `127.0.0.1:5432` 可访问。

### 2.3 启动后端

```powershell
Set-Location C:\Users\ROG\Desktop\Deploy\Deploy11\IntelliDeploy\backend
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

预期结果：
- 控制台无异常退出。
- 数据表自动创建。
- 浏览器访问 `http://127.0.0.1:8000/` 可返回服务状态。

---

## 3. 第一部分：基础冒烟测试

### 3.1 服务根路径

测试命令：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/
```

预期结果：

```json
{"message":"IntelliDeploy API is running"}
```

判定标准：
- 返回 200。
- 返回体包含 `message`。

---

## 4. 第二部分：认证链路测试

### 4.1 注册用户

测试命令：

```powershell
$registerBody = @{
  username = "mvpuser"
  email = "mvpuser@example.com"
  password = "Passw0rd!"
} | ConvertTo-Json

Invoke-RestMethod -Method POST `
  -Uri http://127.0.0.1:8000/auth/register `
  -ContentType "application/json" `
  -Body $registerBody
```

预期结果：
- 返回 201。
- 返回用户信息。
- `username = mvpuser`。

### 4.2 登录获取 Token

测试命令：

```powershell
$loginBody = @{
  username = "mvpuser"
  password = "Passw0rd!"
} | ConvertTo-Json

$login = Invoke-RestMethod -Method POST `
  -Uri http://127.0.0.1:8000/auth/login `
  -ContentType "application/json" `
  -Body $loginBody

$token = $login.access_token
$headers = @{ Authorization = "Bearer $token" }
$token
```

预期结果：
- 返回 `access_token`。
- `token_type = bearer`。
- `$token` 非空。

### 4.3 测试 `/auth/me`

测试命令：

```powershell
Invoke-RestMethod -Method GET `
  -Uri http://127.0.0.1:8000/auth/me `
  -Headers $headers
```

预期结果：
- 返回当前登录用户信息。
- 用户名应为 `mvpuser`。

额外负例测试：

```powershell
Invoke-RestMethod -Method GET `
  -Uri http://127.0.0.1:8000/auth/me
```

预期结果：
- 返回 401。

---

## 5. 第三部分：统一错误结构测试

### 5.1 422 校验错误

测试命令：

```powershell
Invoke-RestMethod -Method POST `
  -Uri http://127.0.0.1:8000/api/generation/start `
  -Headers $headers `
  -ContentType "application/json" `
  -Body '{}'
```

预期结果：
- 返回 422。
- 返回结构包含：

```json
{
  "error": "Request validation failed",
  "code": "VALIDATION_ERROR",
  "details": [...]
}
```

### 5.2 GitHub Token 缺失错误

测试命令：

```powershell
Invoke-RestMethod -Method POST `
  -Uri http://127.0.0.1:8000/api/user/github-token `
  -Headers $headers `
  -ContentType "application/json" `
  -Body '{}'
```

预期结果：
- 返回 400。
- 返回结构包含：

```json
{
  "error": "githubToken is required",
  "code": "GITHUB_TOKEN_MISSING",
  "details": null
}
```

---

## 6. 第四部分：准备最小测试数据

说明：
- `/api/generation/start` 需要有效的 `projectId` 和 `deploymentId`。
- 最省事的方式是直接在数据库里插一组测试数据。

测试命令：

```powershell
@'
from app.database import SessionLocal
from app.models.user import User
from app.models.intellideploy.project import Project
from app.models.intellideploy.deployment import Deployment

db = SessionLocal()
user = db.query(User).filter(User.username == "mvpuser").first()
assert user is not None, "user not found"

project = db.query(Project).filter(Project.repo_name == "mvp-repo", Project.user_id == user.id).first()
if not project:
    project = Project(
        name="MVP Project",
        repo_url="https://github.com/example/mvp-repo",
        repo_owner="example",
        repo_name="mvp-repo",
        visibility="public",
        default_branch="main",
        user_id=user.id,
    )
    db.add(project)
    db.commit()
    db.refresh(project)

deployment = db.query(Deployment).filter(Deployment.project_id == project.id, Deployment.runtime_name == "mvp-app").first()
if not deployment:
    deployment = Deployment(
        project_id=project.id,
        status="pending",
        runtime_name="mvp-app",
    )
    db.add(deployment)
    db.commit()
    db.refresh(deployment)

print({"projectId": project.id, "deploymentId": deployment.id})
db.close()
'@ | .\.venv\Scripts\python.exe -
```

预期结果：
- 成功打印：

```python
{"projectId": 1, "deploymentId": 1}
```

说明：
- 后续命令里的 `projectId`、`deploymentId` 用你实际打印出来的值替换。

---

## 7. 第五部分：Generation REST 接口测试

### 7.1 启动 Generation 任务

测试命令：

```powershell
$genBody = @{
  projectId = "1"
  deploymentId = "1"
  requestId = "sess-mvp-001"
  triggerReason = "FORCE_FALLBACK"
  originalPrompt = "Build a simple FastAPI todo app with health endpoint"
  generationMode = "AUTO"
  repoProfile = @{
    sourceRepoUrl = "https://github.com/example/mvp-repo"
    detectedLanguages = @("Python")
    detectedFrameworks = @("FastAPI")
    entrypoints = @("main.py")
    dependencyFiles = @("requirements.txt")
    readmeSummary = "Simple API repo"
  }
  constraints = @{
    targetPort = 8000
    mustProvideDockerfile = $true
    mustProvideHealthcheck = $true
  }
} | ConvertTo-Json -Depth 8

$gen = Invoke-RestMethod -Method POST `
  -Uri http://127.0.0.1:8000/api/generation/start `
  -Headers $headers `
  -ContentType "application/json" `
  -Body $genBody

$gen
$taskId = $gen.taskId
```

预期结果：
- 返回 200。
- 返回结构至少包含：
  - `accepted`
  - `taskId`
  - `status`
  - `queuedAt`
  - `message`

### 7.2 查询 Generation 状态

测试命令：

```powershell
$statusResp = Invoke-RestMethod -Method GET `
  -Uri "http://127.0.0.1:8000/api/generation/status/$taskId" `
  -Headers $headers

$statusResp
$sessionId = $statusResp.sessionId
```

预期结果：
- 返回 200。
- 返回体使用 camelCase。
- 返回字段应至少包含：
  - `taskId`
  - `projectId`
  - `deploymentId`
  - `status`
  - `currentStage`
  - `progressMessage`
  - `artifactReady`
  - `sessionId`
  - `iterationCount`
  - `isApproved`
  - `failureReason`

### 7.3 查询 Artifact

测试命令：

```powershell
Invoke-RestMethod -Method GET `
  -Uri "http://127.0.0.1:8000/api/generation/artifact/$taskId" `
  -Headers $headers
```

预期结果：
- 如果主图执行成功：
  - 返回 `dockerfileContent`
  - 返回 `runtime`
  - 返回 `requiredEnvs`
  - 返回 `deployReady`
  - 返回 `artifactVersion`
  - 返回 `currentConfigs`
  - 返回 `reviewHistory`
  - 返回 `securityReports`
- 如果执行失败：
  - 应返回统一错误结构 `error/code/details`

---

## 8. 第六部分：WebSocket 会话流测试

### 8.1 测试 `/ws/chat/{sessionId}`

测试命令：

```powershell
@'
import asyncio, json, websockets

TOKEN = r"""REPLACE_TOKEN"""
SESSION_ID = "sess-mvp-001"

async def main():
    uri = f"ws://127.0.0.1:8000/ws/chat/{SESSION_ID}?token={TOKEN}"
    async with websockets.connect(uri) as ws:
        await ws.send(json.dumps({"type": "ping"}))
        print(await ws.recv())
        print(await ws.recv())

asyncio.run(main())
'@ | .\.venv\Scripts\python.exe -
```

说明：
- 把 `REPLACE_TOKEN` 替换成真实登录 token。

预期结果：
- 第一条消息应为：

```json
{"type":"pong"}
```

- 第二条消息应为 `phase_update`，并包含：
  - `sessionId`
  - `taskId`
  - `data.phase`
  - `data.status`
  - `data.progressMessage`
  - `data.deploymentId`
  - `data.iterationCount`
  - `data.isApproved`

如果任务已经结束：
- `data.appCard` 应非空。

### 8.2 观察 `message_append`

当前实现中：
- `message_append` 是把多智能体事件流里的摘要信息同步转成消息流。

建议测试方法：
- 在任务执行完成后再次连接相同 session。
- 观察运行日志或继续扩脚本等待更多消息。

扩展版测试脚本：

```powershell
@'
import asyncio, json, websockets

TOKEN = r"""REPLACE_TOKEN"""
SESSION_ID = "sess-mvp-001"

async def main():
    uri = f"ws://127.0.0.1:8000/ws/chat/{SESSION_ID}?token={TOKEN}"
    async with websockets.connect(uri) as ws:
        await ws.send(json.dumps({"type": "ping"}))
        for _ in range(6):
            msg = await ws.recv()
            print(msg)

asyncio.run(main())
'@ | .\.venv\Scripts\python.exe -
```

预期结果：
- 除 `pong` 和 `phase_update` 外，可能看到：
  - `message_append`
  - `task_done`
  - `task_error`

### 8.3 `task_done` / `task_error`

预期结果：
- 成功时应收到 `task_done`
- 失败时应收到 `task_error`
- 成功结束时 `data.appCard` 至少应包含：
  - `title`
  - `summary`
  - `status`
  - `artifactVersion`
  - `deployReady`
  - `accessUrl`
  - `runtimeName`
  - `ingressDomain`

---

## 9. 第七部分：Deployment WebSocket 测试

### 9.1 测试 `/ws/deployments/{deploymentId}`

测试命令：

```powershell
@'
import asyncio, websockets

TOKEN = r"""REPLACE_TOKEN"""
DEPLOYMENT_ID = 1

async def main():
    uri = f"ws://127.0.0.1:8000/ws/deployments/{DEPLOYMENT_ID}?token={TOKEN}"
    async with websockets.connect(uri) as ws:
        await ws.send("ping")
        print(await ws.recv())
        print(await ws.recv())

asyncio.run(main())
'@ | .\.venv\Scripts\python.exe -
```

预期结果：
- 第一条返回：

```text
pong
```

- 第二条返回部署状态消息：
  - `type = phase_update`
  - 包含 `deploymentId`
  - `data.status` 存在

---

## 10. 第八部分：需要重点观察的失败点

### 10.1 数据库启动失败

现象：
- 后端启动时直接报错。

排查方向：
- `.env` 是否存在
- `DATABASE_URL` 是否正确
- PostgreSQL 是否已启动

### 10.2 注册成功但登录失败

排查方向：
- 用户是否真的写入数据库
- 密码是否一致
- 数据库连接是否连的是同一个库

### 10.3 `/api/generation/start` 报错

排查方向：
- `projectId` / `deploymentId` 是否真实存在
- `graph.py` 依赖文件是否在当前 `DEPLOY_ROOT` 路径下可导入
- 主图内部 agent 逻辑是否抛异常

### 10.4 WebSocket 401 / 1008

排查方向：
- token 是否有效
- query string 是否带了 `?token=...`
- sessionId / deploymentId 是否真实存在

### 10.5 连上 WebSocket 但只有初始消息

说明：
- 这是当前 MVP 阶段可能出现的正常现象。
- 现实现阶段更容易稳定验证“任务完成后的初始快照”，而不是完整实时流式播放。

---

## 11. 当前 MVP 的实际结论标准

如果下面这些都通过，就可以认为你们当前负责的 MVP 基本跑通：

1. 后端能启动。
2. 注册、登录、JWT 鉴权正常。
3. `/api/generation/start` 能成功返回 `taskId`。
4. `/api/generation/status/{taskId}` 返回 camelCase 状态信息。
5. `/ws/chat/{sessionId}` 能连上，能收到 `pong` 和 `phase_update`。
6. 已结束任务的会话初始消息里能看到 `appCard`。
7. `/ws/deployments/{deploymentId}` 能连上，能收到部署状态消息。
8. 422 和业务错误返回符合 `error/code/details` 结构。

---

## 12. 当前限制

当前 MVP 还不是完整比赛级闭环，主要限制包括：

1. `message_append` 现在是事件摘要转发，不是真正聊天消息流。
2. `appCard` 现在是最小结构，不是最终 UI 富卡片结构。
3. `generation/start` 当前更接近同步执行主图，不是完整异步任务编排。
4. deployment 全链路仍依赖外部环境，例如 Sealos、镜像构建、K8s 配置等。

如果只以你们当前分工为准：
- 多智能体状态机
- WebSocket 会话流
- 接口对齐与最小联调

那么这份测试链路已经足够覆盖当前交付范围。
