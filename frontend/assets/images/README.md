## Image Assets

UI-facing images should be placed under `frontend/assets/images/ui/`.

Current convention:

- `ui/`: screen and component images used directly in the app interface

Keep existing files in place unless a screen is being actively refactored, then move the related assets together and update imports in the same change.
