# 开发日志

时间戳：2026-05-09 23:59 (Asia/Shanghai)

## 本次已完成

### 1. 已将主目录多智能体执行入口接入 `Deploy11\IntelliDeploy` 的生成主链

接入位置：

- `Deploy11\IntelliDeploy\backend\app\services\generation_task_service.py`

已做的事情：

- 让 `POST /api/generation/start` 对应的服务层优先尝试调用主目录的 `run_graph(state)`
- 如果主目录 graph 可用，则直接执行主目录多智能体流程
- 如果主目录 graph 不可用或执行失败，则自动回退到原有 fallback 逻辑

### 2. 已让主目录 graph 的执行结果可以通过现有接口返回

修改位置：

- `Deploy11\IntelliDeploy\backend\app\schemas\fallback.py`

已补充的返回字段：

- `session_id`
- `iteration_count`
- `is_approved`
- `failure_reason`
- `artifact_version`
- `current_configs`
- `review_history`
- `security_reports`

影响到的接口：

- `GET /api/generation/status/{task_id}`
- `GET /api/generation/artifact/{task_id}`

### 3. 已把主目录 graph 的事件接到现有 WebSocket 管理器

修改位置：

- `Deploy11\IntelliDeploy\backend\app\services\websocket_manager.py`

已做的事情：

- 新增 `broadcast_agent_state(...)`
- 用于发送多智能体状态流事件
- 接收主目录 graph 的：
  - `stage`
  - `message`
  - `iteration_count`
  - `event_type`

### 4. 已把主目录 graph 的事件写入部署事件表

修改位置：

- `Deploy11\IntelliDeploy\backend\app\services\generation_task_service.py`

已做的事情：

- 将 `graph.py` 里的 `event_stream` 转写为 `deployment_events`
- 这样后续可以通过现有事件查询接口看到 graph 执行过程

### 5. 已完成语法级检查

已验证：

- 修改后的 schema 文件通过 `py_compile`
- 修改后的 websocket 管理器通过 `py_compile`
- 修改后的 generation task service 通过 `py_compile`
- generation 路由、deployments 路由、websocket 路由也通过 `py_compile`

说明：

- 目前完成的是“静态可编译接入”
- 还不是“完整运行联调完成”

---

## 当前状态

现在系统已经具备：

1. 生成接口可以优先走主目录多智能体 graph
2. graph 结果可以通过现有状态查询接口读出来
3. graph 产物可以通过现有 artifact 接口读出来
4. graph 事件可以进入现有 deployment websocket 和 deployment events

换句话说：

- 主目录多智能体代码已经不是孤立原型
- 已经被挂进 `Deploy11\IntelliDeploy` 的生成主链

---

## 还没有完成的部分

### 1. 数据模型还没有完全收口

目前虽然把 graph 数据写进了 `generation_tasks`，但还没有新增独立数据库字段来存：

- `session_id`
- `artifact_version`
- `graph_state_snapshot`
- `failure_reason`

当前做法是：

- 先把这部分信息挂进 `constraints` / `repo_profile` / `runtime_info._graph`

这能跑，但不是最终理想结构。

### 2. 主目录 stage 命名与 `Deploy11` 仍未统一

现在仍然存在三套 stage 风格：

- 主目录：`thinking` / `building` / `reviewing` / `security_checking`
- `src/agents`：`Thinking` / `Building` / `SecurityCheck`
- contracts：`THINKING` / `BUILDING` / `SECURITY_CHECK`

这部分还没统一。

### 3. WebSocket 只是把 graph 事件接进去了，但前端是否已消费还没验证

也就是说：

- 后端现在能发
- 但前端有没有按这个格式接，还没联调确认

### 4. 还没做运行级验证

还没完成：

- 启动完整 FastAPI 服务
- 真正调用 `POST /api/generation/start`
- 真正查询 status / artifact
- 真正连 websocket 看消息

---

## 下一步要做什么

### P1. 先做运行联调

要做：

1. 启动 `Deploy11\IntelliDeploy\backend\app`
2. 直接模拟调用 `POST /api/generation/start`
3. 检查是否真的走进主目录 `run_graph`
4. 检查 `GET /api/generation/status/{task_id}` 返回值
5. 检查 `GET /api/generation/artifact/{task_id}` 返回值
6. 检查 websocket 是否收到 agent 状态消息

### P2. 收口数据库字段

要做：

- 在 `generation_task` 模型里补正式字段：
  - `session_id`
  - `artifact_version`
  - `iteration_count`
  - `is_approved`
  - `failure_reason`

这样后面就不用把 graph 元数据塞进 JSON 字段。

### P3. 统一 stage 命名

建议统一为一套内部标准，例如：

- `THINKING`
- `BUILDING`
- `REVIEWING`
- `SECURITY_CHECK`
- `HEALING`
- `APPROVED`
- `FAILED`

### P4. 决定是否继续接 `src/agents` 那套图

当前接入的是：

- 主目录 `graph.py`

还没接入的是：

- `Deploy11\IntelliDeploy\src\agents\multi_agent_graph.py`

后面需要决定：

- 是继续主目录 graph 为主
- 还是迁移到 `src/agents` 那套更完整的图

---

## 一句话总结

本次已经完成“把主目录多智能体 graph 挂进 `Deploy11\IntelliDeploy` 的生成主链”的第一步。  
下一步应该先做真实接口联调，再收口数据库字段和 stage 命名。


## 2026-05-10 00:13 (Asia/Shanghai)

### 本次完成
- 修复 `Deploy11\IntelliDeploy\backend\app\services\healing_engine.py`
- 自愈触发时，先按 `deployment_id` 查最近一次 generation task，并把该 `task_id` 作为 `source_task_id` 传入 `send_deploy_failure_feedback`
- 并行自愈 `parallel_healing(...)` 同步补上同样的 `source_task_id` 传递逻辑
- 修复 `Deploy11\IntelliDeploy\backend\app\routers\intellideploy\deployments.py`
- 创建新 `deployment` 后，立刻把当前 generation task 的 `deployment_id` 回写到新 deployment，避免后续部署失败时查不到原始任务

### 直接结果
- 主文件夹 `graph.py` 接入后的任务，现在可以沿着 `generation task -> deployment -> healing rerun` 继续闭环
- 这一步仍然没有改数据库表结构，只是补齐现有服务层链路

### 下一步
- 继续做一次 `py_compile` 语法校验
- 如果语法通过，再补一轮运行态验证，重点看：
- `POST /api/generation/start`
- `POST /api/deployments/start`
- 部署失败后的 healing 是否会重新走主图任务


## 2026-05-10 00:34 (Asia/Shanghai)

### 本次完成
- 扩展 `Deploy11\IntelliDeploy\backend\app\models\intellideploy\generation_task.py`
- 为 generation task 补了多智能体相关字段：
- `execution_engine`
- `session_id`
- `iteration_count`
- `is_approved`
- `artifact_version`
- `failure_reason`
- `graph_state_snapshot`
- 新增 `generation_task_agent_event.py`
- 新增 `generation_task_artifact_version.py`
- 扩展 `fallback.py`
- 状态接口和 artifact 接口增加 `execution_engine`
- 新增 `AgentEventResponse`
- 扩展 `websocket_manager.py`
- 增加 session 级连接池和广播能力
- 新增 `broadcast_agent_state_by_session(...)`
- 扩展 `routers\websocket.py`
- 新增 `WS /ws/sessions/{session_id}`
- 扩展 `generation_task_service.py`
- 主图任务现在会持久化 agent 事件
- 主图任务现在会持久化 artifact version 摘要
- 主图任务现在会同时向 deployment 通道和 session 通道广播事件
- 扩展 `routers\intellideploy\generation.py`
- 新增：
- `GET /api/generation/task/{task_id}/agent-events`
- `GET /api/generation/session/{session_id}/agent-events`

### 验证结果
- 上述改动已通过 `py_compile`

### 当前仍未完成
- 还没有正式数据库迁移脚本
- 当前 `backend` 下没有现成 `alembic` / `migrations` 目录
- 所以模型字段和新表已经补进代码，但真实数据库还需要后续建表/迁移

### 下一步
- 如果要继续落地，下一步应做真实数据库迁移
- 然后跑一次 FastAPI 启动和接口联调
- 最后再验证前端是否能通过 `session_id` 订阅多智能体事件
