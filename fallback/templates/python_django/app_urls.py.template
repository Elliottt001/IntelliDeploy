from django.http import JsonResponse
from django.urls import path


def index(_request):
    return JsonResponse({"app": "{{app_name}}"})


def health(_request):
    return JsonResponse({"ok": True})


urlpatterns = [
    path("", index),
    path("health", health),
]
