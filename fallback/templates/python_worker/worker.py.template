from fastapi import FastAPI

app = FastAPI(title="{{app_name}}")


@app.get("/health")
def health():
    return {"ok": True}


@app.get("/")
def index():
    return {"app": "{{app_name}}", "mode": "worker"}
